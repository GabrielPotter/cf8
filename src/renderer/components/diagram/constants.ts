export const PORT_SIZE = 14;           // px
export const PORT_CUT = PORT_SIZE / 2 + 6; // stop the edge this far before the port
export const ARROW_SIZE = 8;           // px (arrow length)
export const ARROW_HALF = 3.5;         // px (arrow half-base width)

export const DEFAULT_EDGE_STROKE = "#fff"; // readable on dark backgrounds
